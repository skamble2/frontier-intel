# Evaluation report

Generated by `python3 -m fli.cli evaluate`. Every number and figure here is reproducible by re-running that one command — nothing is hand-copied.

**Metric tiers.** F1/precision/recall are only reported where ground truth exists: known by construction (synthetic) or against a stated human reference. On real data with no gold standard this report gives AGREEMENT, never "accuracy". Each figure carries its tier.

## Corpus

| quantity | value |
|---|---|
| documents | 1257 |
| evidence spans | 2032 |
| insights (events) | 668 |
| clustered | 668 |
| feature rows | 12692 |
| pairwise labels | 950 |
| scored events | 6680 |
| tracked labs | 8 |
| tracked people | 141 |
| LLM spend (USD) | 18.12 |

## Pairwise labelers

| labeler | judgements | ties |
|---|---|---|
| `llm:gpt-5.2/investment/r1` | 300 | 0 |
| `llm:claude-sonnet-5/technical/r1` | 300 | 0 |
| `llm:claude-sonnet-5/investment/r1` | 300 | 0 |
| `human:soham/investment/r1` | 50 | 7 |

## Figures

### Signal-vs-noise funnel

![Signal-vs-noise funnel](f1_funnel.png)

*MECHANICAL — arithmetic over the database, no labels needed*

1257 documents fetched; 297 survived filtering (23.6%), yielding 668 events (2.25 per surviving document).

### Feature correlation

![Feature correlation](f2_feature_correlation.png)

*MECHANICAL — arithmetic over the database, no labels needed*

19 features over 668 events; most correlated pair: source_type_blog / source_type_newsroom.

### Event types per lab

![Event types per lab](f3_event_types_by_lab.png)

*MECHANICAL — arithmetic over the database, no labels needed*

most common: release (215); rarest: personnel (7).

### Cost per task

![Cost per task](f4_cost_by_task.png)

*MECHANICAL — arithmetic over the database, no labels needed*

$18.12 total across 4021 calls.

### Lexicon vs LLM classifier

![Lexicon vs LLM classifier](f5_lexicon_vs_classifier.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

keyword lexicon F1=0.333 · LLM classifier F1=0.571 — scored on 29 labels, none human-audited, under policy v3. Agreement with a stated labeler, not accuracy; a lexicon edit moves these numbers by design.

### Labeler reliability (Dawid-Skene)

![Labeler reliability (Dawid-Skene)](f6_labeler_reliability.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

Rubric `investment` only: 300 pairs x 3 labelers across 3 independent model families (human:soham, llm:claude-sonnet-5, llm:gpt-5.2). Estimated accuracy: human:soham/investment/r1 0.790, llm:gpt-5.2/investment/r1 0.843, llm:claude-sonnet-5/investment/r1 0.898. Reliability is inferred from disagreement between families, so the figure is not produced from a single family — and labelers working to a DIFFERENT rubric are excluded, since they are estimating a different truth and would be scored as unreliable for disagreeing.

### Bake-off

![Bake-off](f7_bakeoff.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

378 labels, 116 held-out pairs; winner logistic.

### Feature ablation

![Feature ablation](f8_ablation.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

features that matter: corroboration, specificity.

### Per-lab fairness

![Per-lab fairness](f9_per_lab_fairness.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

spread 0.400 across 7 labs (INVESTIGATE — the ranking serves Meta AI (0.60) markedly worse than DeepSeek (1.00)).

### Overfitting (train vs held-out)

![Overfitting (train vs held-out)](f10_overfitting.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

largest gap gbm_sklearn +0.011 — gap is small; the held-out number is trustworthy.

### Learning curve

![Learning curve](f11_learning_curve.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

last step 80->160 pairs moved accuracy +0.066; still climbing — more labels are worth buying.

### Rank skew by lab

![Rank skew by lab](f12_rank_skew.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

most over-represented: DeepSeek at 2.5x its corpus share (6% of events, 14% of the top 50). Not rated: xAI (<10 events; a ratio on so few is noise).

### Rubric divergence

![Rubric divergence](f13_rubric_divergence.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

`investment` and `technical` share 2 of their top 10 (24% of the top 25) and rank the corpus at Kendall tau +0.107 — near zero, i.e. the two orderings are close to unrelated. Same events, same features, same clustering; only the definition of 'important' differs. This is the measurement behind the claim that one ranking cannot serve both readers.

### Talent mobility mechanism

![Talent mobility mechanism](f14_mobility.png)

*MECHANICAL — arithmetic over the database, no labels needed*

7 personnel event(s) came from document extraction; 0 were synthesized from affiliation history (a person observed at two labs in succession). 46 of 141 tracked people are re-observable (23 via lab pages, 40 via X bios on a weekly cadence), and 141 were observed inside the last 30 days. The mechanism itself is validated end-to-end in the test suite (tests/knowledge/test_mobility.py plants a move and shows the resulting event reach the digest slate, dated by its arrival) — the live corpus simply has not yet witnessed a move.
