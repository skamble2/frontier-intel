# Evaluation report

Generated by `python3 -m fli.cli evaluate`. Every number and figure here is reproducible by re-running that one command — nothing is hand-copied.

**Metric tiers.** F1/precision/recall are only reported where ground truth exists: known by construction (synthetic) or against a stated human reference. On real data with no gold standard this report gives AGREEMENT, never "accuracy". Each figure carries its tier.

## Corpus

| quantity | value |
|---|---|
| documents | 1142 |
| evidence spans | 1764 |
| insights (events) | 556 |
| clustered | 556 |
| feature rows | 10564 |
| pairwise labels | 900 |
| scored events | 5560 |
| tracked labs | 8 |
| tracked people | 63 |
| LLM spend (USD) | 17.43 |

## Pairwise labelers

| labeler | judgements | ties |
|---|---|---|
| `llm:gpt-5.2/investment/r1` | 300 | 0 |
| `llm:claude-sonnet-5/technical/r1` | 300 | 0 |
| `llm:claude-sonnet-5/investment/r1` | 300 | 0 |

## Figures

### Signal-vs-noise funnel

![Signal-vs-noise funnel](f1_funnel.png)

1142 documents fetched; 268 survived filtering (23.5%), yielding 556 events (2.07 per surviving document).

### Feature correlation

![Feature correlation](f2_feature_correlation.png)

19 features over 556 events; most correlated pair: recency / source_type_newsroom.

### Event types per lab

![Event types per lab](f3_event_types_by_lab.png)

most common: release (177); rarest: personnel (7).

### Cost per task

![Cost per task](f4_cost_by_task.png)

$17.43 total across 3953 calls.

### Lexicon vs LLM classifier

![Lexicon vs LLM classifier](f5_lexicon_vs_classifier.png)

keyword lexicon F1=0.333 · LLM classifier F1=0.571 — scored on 29 labels, none human-audited, under policy v3. Agreement with a stated labeler, not accuracy; a lexicon edit moves these numbers by design.

### Labeler reliability (Dawid-Skene)

![Labeler reliability (Dawid-Skene)](f6_labeler_reliability.png)

Rubric `investment` only: 300 pairs x 2 labelers across 2 independent model families (llm:claude-sonnet-5, llm:gpt-5.2). Estimated accuracy: llm:gpt-5.2/investment/r1 0.855, llm:claude-sonnet-5/investment/r1 0.885. Reliability is inferred from disagreement between families, so the figure is not produced from a single family — and labelers working to a DIFFERENT rubric are excluded, since they are estimating a different truth and would be scored as unreliable for disagreeing.

### Bake-off

![Bake-off](f7_bakeoff.png)

378 labels, 116 held-out pairs; winner logistic.

### Feature ablation

![Feature ablation](f8_ablation.png)

features that matter: recency, specificity.

### Per-lab fairness

![Per-lab fairness](f9_per_lab_fairness.png)

spread 0.400 across 7 labs (INVESTIGATE — the ranking serves Meta AI (0.60) markedly worse than DeepSeek (1.00)).

### Overfitting (train vs held-out)

![Overfitting (train vs held-out)](f10_overfitting.png)

largest gap logistic -0.045 — held-out scores ABOVE train, which is not a good sign but a noisy one: on 116 held-out pairs the split was easier than the training half. Treat the accuracy as approximate and buy more labels before trusting it.

### Learning curve

![Learning curve](f11_learning_curve.png)

last step 80->160 pairs moved accuracy +0.071; still climbing — more labels are worth buying.

### Rank skew by lab

![Rank skew by lab](f12_rank_skew.png)

most over-represented: DeepSeek at 2.4x its corpus share (7% of events, 16% of the top 50). Not rated: xAI (<10 events; a ratio on so few is noise).
