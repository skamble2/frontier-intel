# Evaluation report

Generated by `python3 -m fli.cli evaluate`. Every number and figure here is reproducible by re-running that one command — nothing is hand-copied.

**Metric tiers.** F1/precision/recall are only reported where ground truth exists: known by construction (synthetic) or against a stated human reference. On real data with no gold standard this report gives AGREEMENT, never "accuracy". Each figure carries its tier.

## Corpus

| quantity | value |
|---|---|
| documents | 1541 |
| evidence spans | 2294 |
| insights (events) | 734 |
| clustered | 734 |
| feature rows | 14680 |
| pairwise labels | 2159 |
| scored events | 11010 |
| tracked labs | 8 |
| tracked people | 285 |
| LLM spend (USD) | 25.02 |

## Pairwise labelers

| labeler | judgements | ties |
|---|---|---|
| `llm:claude-sonnet-5/technical/r1` | 508 | 0 |
| `llm:gpt-5.2/investment/r1` | 462 | 0 |
| `llm:claude-sonnet-5/investment/r1` | 462 | 0 |
| `human:soham/investment/r1` | 363 | 40 |
| `llm:gpt-5.2/technical/r1` | 300 | 0 |
| `human:soham/technical/r1` | 64 | 11 |

## Figures

### Signal-vs-noise funnel

![Signal-vs-noise funnel](f1_funnel.png)

*MECHANICAL — arithmetic over the database, no labels needed*

1197 content documents fetched; 316 survived filtering (26.4%), yielding 734 events (2.32 per surviving document). 344 register document(s) (identity-evidence pages) are excluded from every bar — they are fetched to prove who someone is, not to yield insights.

### Feature correlation

![Feature correlation](f2_feature_correlation.png)

*MECHANICAL — arithmetic over the database, no labels needed*

20 features over 734 events; most correlated pair: source_type_blog / source_type_newsroom.

### Event types per lab

![Event types per lab](f3_event_types_by_lab.png)

*MECHANICAL — arithmetic over the database, no labels needed*

most common: release (221); rarest: personnel (8).

### Cost per task

![Cost per task](f4_cost_by_task.png)

*MECHANICAL — arithmetic over the database, no labels needed*

$25.02 total across 7285 calls.

### Lexicon vs LLM classifier

![Lexicon vs LLM classifier](f5_lexicon_vs_classifier.png)

*vs HUMAN REFERENCE — agreement, not accuracy*

keyword lexicon F1=0.267 · LLM classifier F1=0.444 — scored on 100 labels, all human-audited, under policy v3. Agreement with a human-audited reference.

### Labeler reliability (Dawid-Skene)

![Labeler reliability (Dawid-Skene)](f6_labeler_reliability.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

Rubric `investment` only: 462 pairs x 3 labelers across 3 independent model families (human:soham, llm:claude-sonnet-5, llm:gpt-5.2). Estimated accuracy: human:soham/investment/r1 0.778, llm:claude-sonnet-5/investment/r1 0.864, llm:gpt-5.2/investment/r1 0.874. Reliability is inferred from disagreement between families, so the figure is not produced from a single family — and labelers working to a DIFFERENT rubric are excluded, since they are estimating a different truth and would be scored as unreliable for disagreeing. The human's estimate is depressed by adverse selection, not skill: human votes come from the audit, disagreement and near-tie queues — deliberately the hardest pairs — while the LLM labelers vote on the whole random sample, so the two numbers are not on the same difficulty scale.

### Bake-off

![Bake-off](f7_bakeoff.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

580 labels, 179 held-out pairs; winner logistic at 0.821 held-out accuracy (chance is 0.5; every model must beat the baselines shown grey to earn its complexity).

### Feature ablation

![Feature ablation](f8_ablation.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

features that matter: event_type_benchmark, event_type_other, attribution_confidence, specificity.

### Per-lab fairness

![Per-lab fairness](f9_per_lab_fairness.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

raw p@10 spread 0.300, but raw p@10 tracks each lab's base rate, so LIFT is the fairness number: OpenAI +0.53, Qwen +0.49, Google DeepMind +0.46, Mistral +0.44, DeepSeek +0.41, Anthropic +0.40, Meta AI +0.16. Weakest lift: Meta AI (+0.16 on a 0.54 base, n=35) — its top-10 misses are infrastructure x1, open_source x1, release x1: events whose feature shape (official-channel engineering posts, high specificity) the score rewards but the judges call irrelevant. A feature-shape gap, not lab-identity bias (lab is never a feature). Lift is also CEILINGED at 1-base, so the fair per-lab comparison is lift/ceiling: OpenAI 84%, Qwen 71%, Google DeepMind 82%, Mistral 69%, DeepSeek 100%, Anthropic 57%, Meta AI 34%.

### Overfitting (train vs held-out)

![Overfitting (train vs held-out)](f10_overfitting.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

largest gap gbm_sklearn +0.075 — gap is small; the held-out number is trustworthy.

### Learning curve

![Learning curve](f11_learning_curve.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

last step 160->320 pairs moved accuracy +0.038; still climbing — more labels are worth buying.

### Rank skew by lab

![Rank skew by lab](f12_rank_skew.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

most over-represented: DeepSeek at 2.4x its corpus share (5% of events, 12% of the top 50). Not rated: xAI (<10 events; a ratio on so few is noise).

### Rubric divergence

![Rubric divergence](f13_rubric_divergence.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

`investment` and `technical` share 0 of their top 10 (8% of the top 25) and rank the corpus at Kendall tau +0.064 — near zero, i.e. the two orderings are close to unrelated. Same events, same features, same clustering; only the definition of 'important' differs. This is the measurement behind the claim that one ranking cannot serve both readers.

### Talent mobility mechanism

![Talent mobility mechanism](f14_mobility.png)

*MECHANICAL — arithmetic over the database, no labels needed*

8 personnel event(s) came from document extraction; 0 were synthesized from affiliation history (a person observed at two labs in succession). 190 of 285 tracked people are re-observable (23 via lab pages, 40 via X bios on a weekly cadence), and 285 were observed inside the last 30 days. The mechanism itself is validated end-to-end in the test suite (tests/knowledge/test_mobility.py plants a move and shows the resulting event reach the digest slate, dated by its arrival) — the live corpus simply has not yet witnessed a move. Profile re-observation runs on a 7-day cadence and the most recent observation landed 2026-07-30, so the earliest date a cadence-gated move can be witnessed live is 2026-08-06 — the zero here is arithmetic, not omission.

### Claim faithfulness (entailment)

![Claim faithfulness (entailment)](f15_faithfulness.png)

*JUDGED — against an unaudited LLM reference; treat as provisional*

Every quote is byte-verified against its source (check C2), so this measures the remaining hallucination surface: whether the extracted claim is supported by the quote ALONE. claude-haiku-4-5-20251001: 699/734 entailed (95.2%), 24 partial, 11 not entailed. `partial` names a load-bearing fact (a number, a date, an actor) the quote does not carry — the actionable failure mode for an extraction prompt revision. That revision has since been made and measured (see docs/prompts.md): a quote-first instruction block cut the partial rate on the 30 hardest documents from 65.6% to 47.9% in a seeded A/B re-extraction; it applies to new extractions, so the corpus numbers above are the pre-revision audit. Verification tier ledger: 734/734 insight quotes are exact-tier (byte-verbatim under the shared normalization; C19 enforces this stays 100%). The corpus's 1255 structural rows carry no claims — they are the register's author-name spans in structured documents, where structural IS the designed tier. Of the 43 quotes the verifier REJECTED (never shown to a reader): 20 are >=80%-contiguous near-misses — the verifier being stricter than the normalization, not model failures; 21 are paraphrases; only 2 are fabrications with no anchor in the source. The floor metric is really a characterized rejection ledger.

### Digest slate precision@k

![Digest slate precision@k](f16_slate_precision.png)

*vs HUMAN REFERENCE — agreement, not accuracy*

Each delivered digest item was marked keep or cut by a human reader (`digest --review`), which is precision@k for the system's central question. ai_team: 12/14 kept (86%); investment: 10/19 kept (53%). A cut item names the noise the slate rules let through; the review is per persona because the two audiences call different things noise. The investment cuts are the failure class f9 diagnosed — vendor case studies and official-channel engineering posts whose feature shape (official source, high specificity) the score rewards but the reader rejects — so the two figures corroborate each other from independent human reads.

### Planted-policy recovery

![Planted-policy recovery](f17_synthetic_recovery.png)

*SYNTHETIC — ground truth known by construction*

Four known weight vectors of increasing difficulty are planted over the real standardized feature matrix (real, so recovery must survive the actual feature correlations), 400 pairwise labels are generated from each with 10% of verdicts flipped — the same order of unreliability f6 measured in the real judge — and the bake-off's own training path (antisymmetric pair differences, intercept-free logistic) is asked for the planted ranking back. single_feature: AUC 0.99, F1 0.91; hand_shape: AUC 0.97, F1 0.91; dense_mixed: AUC 0.95, F1 0.84; anti_prior: AUC 0.97, F1 0.89. `anti_prior` puts a NEGATIVE weight on recency, so no baseline shape recovers it by luck; its recovery shows the machinery follows the labels, not the priors. This validates the machinery, not the product: it says that IF reader preferences are near-linear in these features, the pipeline finds them through judge-level noise — whether the judge's preferences are the READER's is what f6 and f16 measure.
