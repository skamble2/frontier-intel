# Core — the pipeline will not run without these.
anthropic>=0.117
pydantic>=2.13
pyyaml>=6.0
numpy>=1.26
scikit-learn>=1.3

# Second model family. Lazily imported, only when a task routes to an OpenAI
# model. Dawid-Skene needs conditionally independent labelers, and prompt
# variants of one model are not independent (measured: 92-100% agreement).
openai>=1.40

# Figures, web UI, graph runner, agent surface. Each is imported inside the
# function that needs it, so the daily pipeline runs without any of them.
matplotlib>=3.7
seaborn>=0.13
pandas>=2.0
flask>=3.0
langgraph>=1.0
mcp>=2.0

# --- tracing (optional) ---------------------------------------------------
# Uncomment the two lines below, then run with FLI_TRACING=1. Client libraries
# only; tracing is a no-op when they are absent.
#
# The Phoenix viewer is a heavy server — run it isolated rather than installing
# it here:  docker run -p 6006:6006 arizephoenix/phoenix:latest
#
# opentelemetry-sdk
# opentelemetry-exporter-otlp-proto-http
